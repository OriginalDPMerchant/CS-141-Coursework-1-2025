The search system tokenizes the user query into individual words.
It removes common stopwords (e.g., "the," "is," "and") unless they are essential to the query.
Google supports boolean operators (e.g., AND, OR, NOT), phrase matching (e.g., "climate change"), and other refinements.